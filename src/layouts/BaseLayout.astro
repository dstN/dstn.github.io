---
interface Props {
  title?: string;
  description?: string;
}

import "../styles/global.css";
import "../styles/animations.css";
import "../styles/components.css";

const {
  title = "dstN. | Frontend Developer",
  description = "Portfolio of dstN — Frontend Developer from Hamburg. Building clean, performant, and accessible web experiences.",
} = Astro.props;

// Derived per route so /work does not claim the homepage as its canonical.
const canonical = new URL(Astro.url.pathname, Astro.site ?? "https://dstn.github.io").href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={canonical} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="dstN Portfolio" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content="Frontend Developer from Hamburg. Clean code, modern web." />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="dstN" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Theme -->
    <meta name="theme-color" content="#1a2920" />

    <!-- Font preloads (critical weights only) -->
    <link rel="preload" href="/fonts/lexend/lexend-400.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/lexend/lexend-500.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/lexend/lexend-600.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/lexend/lexend-700.woff2" as="font" type="font/woff2" crossorigin />

    <!-- Theme detection — prevents FOUC -->
    <!-- Default = dark (no class). "light" class = light mode. -->
    <script is:inline>
      (function(){
        try {
          var t = localStorage.getItem("theme");
          if (t === "light" || (!t && window.matchMedia("(prefers-color-scheme:light)").matches)) {
            document.documentElement.classList.add("light");
          }
        } catch(e) {}
      })()
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
